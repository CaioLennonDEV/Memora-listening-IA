### Live roster — Wave 2 Teams + Wave 3 Zoom

Same SSE `type:"roster"` contract as Google Meet (Wave 1). Teams uses tile surfaces + open roster panel; Zoom uses avatar name footers. Alone-leave reuses `BOT_ROSTER_ALONE_DEBOUNCE_MS`. Rebuild the SonicWALL bot overlay (`build-roster-overlay.cmd`) so `browser-utils.global.js` includes both `onRoster` paths.
